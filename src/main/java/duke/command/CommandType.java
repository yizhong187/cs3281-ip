package duke.command;

/**
 * Enumerates all supported command types in Aria.
 */
public enum CommandType {
    BYE, LIST, MARK, UNMARK, DELETE, TODO, DEADLINE, EVENT, FIND, HELP, SORT, UPDATE,
    TAG, UNTAG, ARCHIVE, ARCHIVES, STATS, UNDO, SCHEDULE, SNOOZE, TEVENT, CONFIRM, WITHIN, FIXED,
    FREETIME, NOTE, CONTACT, EXPENSE, LOAN, PLACE, TRIVIA
}
